#config/analysis_map.yaml -
#
# NOTE: conversion routing is handled explicitly in analyzers/selector.py.
# This section is retained for documentation only; it is not used at runtime.

conversion:
  file_pattern: "conversion"
  columns:
    card: "Карта"
    status: "Статус"
    datetime: "Дата/Время создания"
    partner: "Партнер"

payout:
  file_pattern: "payout"
  description: "Анализ выплатных файлов"